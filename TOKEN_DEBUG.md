# Тестирование токена аутентификации

## Проблема
При логине токен не сохраняется в localStorage или cookies.

## Что было сделано

### 1. Добавлен утилит для работы с токеном
- Создан `src/shared/utils/auth-token.ts` с централизованными функциями для работы с токеном
- Все операции с токеном теперь логируются в консоль

### 2. Обновлены хуки аутентификации
- `use-login.ts` - добавлено логирование ответа от сервера и гибкая обработка разных форматов токена
- `use-register.ts` - аналогично для регистрации
- `use-logout.ts` - использует новый утилит для удаления токена

### 3. Обновлен FetchClient
- Автоматически добавляет токен из localStorage в заголовок `Authorization: Bearer {token}`
- Токен добавляется ко всем запросам (кроме логина/регистрации)

## Как проверить

### 1. Откройте DevTools (Console + Application)
- Chrome/Edge: F12 → Console + Application tabs
- Firefox: F12 → Console + Storage tabs

### 2. Попробуйте войти
1. Откройте `/auth/login`
2. Введите email и пароль
3. Нажмите "Login"
4. **Проверьте Console** - должны появиться логи:
   ```
   Login response: {...}
   Login response structure: {...}
   Saving token to localStorage: eyJhbG...
   ✅ Token saved to localStorage
   ```

### 3. Проверьте localStorage
1. DevTools → Application → Local Storage → https://crm-lyart-mu.vercel.app
2. Должен быть ключ `authToken` со значением токена

### 4. Проверьте запросы
1. DevTools → Network
2. Перейдите на любую страницу dashboard
3. Найдите API запросы к `http://localhost:4001`
4. Проверьте Headers → должен быть `Authorization: Bearer {token}`

### 5. Проверьте logout
1. Нажмите Logout
2. В Console должен быть лог: `🗑️ Token removed from localStorage`
3. В localStorage не должно быть ключа `authToken`

## Возможные форматы ответа от бэкенда

Код поддерживает следующие варианты:

```json
// Вариант 1: токен на верхнем уровне
{
  "accessToken": "eyJhbG..."
}

// Вариант 2: токен в поле token
{
  "token": "eyJhbG..."
}

// Вариант 3: токен внутри data
{
  "data": {
    "accessToken": "eyJhbG..."
  }
}

// Вариант 4: токен внутри data.token
{
  "data": {
    "token": "eyJhbG..."
  }
}
```

## Что дальше

Если токен все еще не сохраняется:
1. Проверьте логи в Console - они покажут точную структуру ответа от бэкенда
2. Убедитесь что бэкенд действительно возвращает токен
3. Проверьте что бэкенд не устанавливает токен в httpOnly cookies (в таком случае нужен другой подход)
